# Frontend environment variables
# The frontend uses Vite's proxy, so no API URL needed for local development.
# All /api/* requests are proxied to http://localhost:3001 by vite.config.ts

# Optional: override the socket URL (defaults to window.location.origin)
# VITE_SOCKET_URL=http://localhost:3001
