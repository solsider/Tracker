{
  "name": "tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Tracker — project management system (monorepo root)",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "node dev.js",
    "demo": "node dev.js --seed",
    "prod": "docker compose up --build",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm run install:all",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:e2e": "npm run test --prefix e2e",
    "install:e2e": "npm install --prefix e2e && npx --prefix e2e playwright install --with-deps chromium",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:studio": "npm run db:studio --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "stop": "docker compose -f docker-compose.dev.yml down",
    "staging:up": "docker compose -f docker-compose.staging.yml up -d",
    "staging:seed": "docker compose -f docker-compose.staging.yml --profile seed run seed-staging",
    "staging:down": "docker compose -f docker-compose.staging.yml down",
    "load:smoke": "k6 run load-tests/k6/smoke.js",
    "load:load": "k6 run load-tests/k6/load.js",
    "load:stress": "k6 run load-tests/k6/stress.js",
    "deploy": "bash scripts/deploy.sh",
    "rollback": "bash scripts/rollback.sh",
    "backup": "bash scripts/backup.sh",
    "health": "bash scripts/health-check.sh"
  }
}
